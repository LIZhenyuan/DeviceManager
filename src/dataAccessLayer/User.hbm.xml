<hibernate-mapping>
<!-- 
    <database-object>
        <create>
            CREATE TABLE User(
            	username varchar(30),
            	passwd varchar(30)
            );
        </create>
        <drop>
        	DROP TABLE User;
        </drop>
        <dialect-scope></dialect-scope>
    </database-object>    
-->
	<class name="dataAccessLayer.User">
		<id name="username"></id>
		<property name="passwd"></property>
	</class>
</hibernate-mapping>